@page "/project/{ProjectId:int}"

@inject IThreadRepo _threads

<ThreadListComponent ProjectId="@ProjectId"></ThreadListComponent>

@code {

    [Parameter]
    public int ProjectId { get; set; }
    
}